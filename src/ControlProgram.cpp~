#include <Robot.hpp>

Robot::Robot() : private_nh("~") {

	speeds_publisher = nh.advertise("cmd_vel", 1);

}


Robot::~Robot(){
	speeds_publisher.shutdown();
}



bool Robot::move(){

	ros::Rate loop_rate( 30 );

	while(nh_.ok()){

		ros::spinOnce();
		loop_rate.sleep();
	}
	return true;
}

